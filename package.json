{
  "name": "fieldos-development-workspace",
  "private": true,
  "version": "0.25.0",
  "type": "module",
  "scripts": {
    "start": "./scripts/serve.sh",
    "backup": "python3 scripts/backup.py create",
    "check": "node --check web/app.js && node --check web/data.js && python3 -m unittest discover -s tests -v && python3 scripts/quality_gate.py"
  }
}
